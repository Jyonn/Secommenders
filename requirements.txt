# Quick setup:
#   pip install -r requirements.txt
#
# If you need a specific CUDA wheel, install your desired torch build first,
# then run the command above again.

autoencoders[text,torch]

pandas>=2.0,<3
sentence-transformers>=3,<6
scikit-learn>=1.4,<2
pyyaml>=6,<7
requests>=2,<3
transformers
peft
tqdm
pigmento
refconfig
oba
